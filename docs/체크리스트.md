# 이르미(IRMI) 구현 체크리스트

구현계획서 기준 Phase별 작업 항목. 각 항목 완료 시 `[ ]`를 `[x]`로 변경.

---

## Phase 1: 기반 구축

### 타입 정의 (`lib/types.ts`)

- [x] CategoryKey 타입 (`"prices" | "employment" | "selfEmployed" | "finance" | "realEstate"`)
- [x] Severity 타입 (`"critical" | "warning" | "caution" | "safe"`)
- [x] Trend 타입 (`"rising" | "stable" | "falling"`)
- [x] CategoryRisk 인터페이스 (label, score, trend, keyIssues)
- [x] SignalStats 인터페이스 (critical, warning, caution, surging)
- [x] SignalPreview 인터페이스
- [x] DashboardData 인터페이스
- [x] SignalAnalysis 인터페이스 (cause, impact, actionPoints)
- [x] Signal 인터페이스
- [x] NewsArticle 인터페이스
- [x] RegionRisk 인터페이스
- [x] RegionsData 인터페이스
- [x] ForecastScenario 인터페이스 (type, label, overallScore, description)
- [x] ForecastData 인터페이스 (period, outlook, scenarios)
- [x] BriefingHighlight 인터페이스
- [x] BriefingData 인터페이스 (forecast 필드 포함)
- [x] ChatMessage 인터페이스
- [x] ChatExample 인터페이스
- [x] ChatData 인터페이스
- [x] CrisisNode, CrisisEdge, CrisisChain, CrisisChainData 인터페이스
- [x] Policy 인터페이스 (id, title, provider, contact, url, targetCategories, relatedSignals, eligibility, benefit)
- [x] ActionGuide 인터페이스
- [x] RegionComparison 인터페이스 (regionScore, nationalAverage, difference, rank)
- [x] ReportMeta 인터페이스

### 상수 정의 (`lib/constants.ts`)

- [x] CATEGORIES 배열 (5대 카테고리: key, label, description)
- [x] SEVERITY_CONFIG (4개 등급: key, label, scoreRange, description)
- [x] REGIONS 배열 (전국, 서울, 경기, 인천, 부산, 대구, 광주, 대전)
- [x] PERIOD_OPTIONS 배열 (1w, 1m, 3m)

### 디자인 토큰 (`app/globals.css`)

- [x] `:root`에 `--danger`, `--danger-foreground` 추가
- [x] `:root`에 `--warning`, `--warning-foreground` 추가
- [x] `:root`에 `--caution`, `--caution-foreground` 추가
- [x] `:root`에 `--safe`, `--safe-foreground` 추가
- [x] `.dark`에 위 4개 변수 다크모드 값 추가
- [x] `@theme inline`에 색상 매핑 추가

### Mock 데이터 생성

- [x] `data/mock/dashboard.json` (종합 리스크 데이터)
- [x] `data/mock/signals.json` (위기 신호 7건)
- [x] `data/mock/news.json` (뉴스 기사 10건)
- [x] `data/mock/regions.json` (8개 지역 데이터)
- [x] `data/mock/briefing.json` (브리핑 + forecast 필드 포함)
- [x] `data/mock/chat-examples.json` (대화 예시 3건 + 추천 질문 4개)
- [x] `data/mock/crisis-chain.json` (노드 5개, 엣지 9개, 체인 3개)
- [x] `data/mock/policies.json` (지원 정책 6건)

### 환경변수

- [x] `.env.example` 생성 (ANTHROPIC_API_KEY, NEWS_DATA_PATH, NEXT_PUBLIC_APP_NAME, NEXT_PUBLIC_APP_DESCRIPTION)
- [x] `.env.local`이 `.gitignore`에 포함되어 있는지 확인

---

## Phase 2: 공통 레이아웃

### 필요 shadcn 컴포넌트 확인/설치

- [x] 기존 설치 컴포넌트 확인 (alert-dialog, badge, button, card, combobox, dropdown-menu, field, input, input-group, label, select, separator, textarea)
- [x] tabs 컴포넌트 설치
- [x] progress 컴포넌트 설치
- [x] dialog 컴포넌트 설치 (또는 alert-dialog 활용 확인)
- [x] scroll-area 컴포넌트 설치
- [x] skeleton 컴포넌트 설치
- [ ] toast 컴포넌트 설치

### 레이아웃 컴포넌트

- [x] `components/layout/app-header.tsx` - 서비스 헤더
  - [x] 서비스 로고/이름 (HugeIcon + 텍스트)
  - [x] 서비스 설명 (부제)
  - [x] 기간 필터 (Select: 최근 1주/1개월/3개월)
  - [x] 알림 설정 버튼
- [x] `components/layout/tab-navigation.tsx` - 탭 네비게이션 (app-header에 포함됨)
  - [x] 대시보드 탭
  - [x] 위기 신호 탭 (지역 지도 통합)
  - [x] 뉴스 분석 탭

### 라우트 구조

- [x] `app/layout.tsx` 수정 (루트 레이아웃에 app-header 포함)
- [x] `app/(tabs)/layout.tsx` 생성 (탭 네비게이션 포함)
- [x] `app/(tabs)/page.tsx` 라우트 확인 (대시보드 기본)
- [x] `app/(tabs)/signals/page.tsx` 라우트 생성
- [x] `app/(tabs)/news/page.tsx` 라우트 생성

### 정리

- [x] 기존 `app/page.tsx` 제거 (-> `(tabs)/page.tsx`로 이동)
- [x] `components/component-example.tsx` 삭제
- [x] `components/example.tsx` 삭제

---

## Phase 3: 대시보드 탭 UI + AI 브리핑 + 전망 + 리포트

### AI 브리핑 패널

- [x] `components/dashboard/ai-briefing-panel.tsx` (차별화 #1)
  - [x] 패널 헤더: "오늘의 민생 브리핑" + AI 아이콘 + 생성 시각
  - [x] 브리핑 본문 (summary) + 타이핑 애니메이션
  - [x] 핵심 하이라이트 (highlights) x3 - 카테고리 아이콘 + 메시지
  - [x] 종합 권고 (recommendation)
  - [x] `"use client"` 선언 (타이핑 애니메이션 상태 관리)

### 전망 패널

- [x] `components/dashboard/forecast-panel.tsx`
  - [x] "향후 전망" 소제목 + 기간 표시
  - [x] 전망 텍스트 (outlook) - 타이핑 효과
  - [x] 시나리오 비교 카드 2개 (현재 추세 vs 선제 대응)
  - [x] 각 카드에 예상 점수 미니 게이지 + 설명

### 대시보드 헤더 + 리포트 다운로드

- [x] `components/dashboard/dashboard-header.tsx`
  - [x] "종합 민생 리스크 지수" 제목
  - [x] 기준일시 표시 (lastUpdated)
- [x] `components/dashboard/report-download-button.tsx`
  - [x] "리포트 다운로드" 버튼 (HugeIcon + 텍스트)
  - [x] `window.print()` 호출 로직
  - [x] `"use client"` 선언
- [x] `app/globals.css`에 `@media print` 스타일 추가
  - [x] 헤더/탭 숨김
  - [x] 데이터 영역 정리된 레이아웃

### 대시보드 요약 영역

- [x] `components/dashboard/dashboard-summary.tsx` (게이지 + 통계 래퍼)
- [x] `components/dashboard/risk-gauge.tsx`
  - [x] 원형 게이지 (SVG 또는 CSS)
  - [x] 점수 숫자 크게 표시
  - [x] 등급별 색상 변경 (danger/warning/caution/safe 토큰)
- [x] `components/dashboard/signal-stats.tsx`
  - [x] 긴급 신호 N건 (danger 색상)
  - [x] 주의 신호 N건 (warning 색상)
  - [x] 급상승 이슈 N건

### 카테고리 위험도

- [x] `components/dashboard/category-risk-list.tsx` (목록 래퍼)
- [x] `components/dashboard/category-risk-bar.tsx` (x5)
  - [x] 카테고리 아이콘 (HugeIcon)
  - [x] 카테고리명
  - [x] 점수 바 (프로그레스 바, 등급 색상)
  - [x] 점수 숫자
  - [x] 추세 아이콘 (상승/보합/하락)

### 최근 신호 미리보기

- [x] `components/dashboard/recent-signals.tsx` (목록 래퍼)
- [x] `components/dashboard/signal-preview-card.tsx` (x3)
  - [x] 등급 배지
  - [x] 카테고리 라벨
  - [x] 신호 제목
  - [x] 감지 날짜

### 페이지 조립

- [x] `app/(tabs)/page.tsx` - Server Component에서 dashboard.json + briefing.json import
- [x] `components/dashboard/dashboard-page.tsx` - 메인 레이아웃 컴포넌트
- [x] 데이터 흐름 확인: page -> dashboard-page -> 하위 컴포넌트 props 전달

---

## Phase 4: 위기 신호 탭 UI + 대응 가이드

### 필터 바

- [x] `components/signals/signals-filter-bar.tsx`
  - [x] 카테고리 필터 (Select: 전체/물가/고용/자영업/금융/부동산)
  - [x] 지역 필터 (Select: 전체/서울/경기/인천/...)
  - [x] 등급 필터 (추가 구현: 전체/긴급/주의/관찰/안전)

### 신호 목록

- [x] `components/signals/signal-list.tsx` (목록 래퍼)
- [x] `components/signals/signal-card.tsx`
  - [x] 상단: 등급 배지 + 카테고리 라벨 + 지역
  - [x] 중단: 신호 제목 (볼드) + 설명 텍스트
  - [x] 하단: 관련 기사 N건 + 감지 날짜
  - [x] "상세 분석 보기" 버튼

### 상세 분석 다이얼로그

- [x] `components/signals/signal-detail-dialog.tsx`
  - [x] 신호 제목
  - [x] 근거 목록 (evidence)
  - [x] 원인 분석 (cause)
  - [x] 영향 범위 (impact)
  - [x] 대응 포인트 (actionPoints)

### 대응 가이드 + 정책 연결

- [x] `components/signals/action-guide-section.tsx`
  - [x] "대응 가이드" 소제목
  - [x] 실질적 행동 요령 목록 (단계별 안내)
  - [x] "관련 지원 정책" 소제목
  - [x] 정책 매칭 로직 (relatedSignals > targetCategories)
- [x] `components/signals/policy-card.tsx`
  - [x] 정책명 (title)
  - [x] 지원 기관 (provider)
  - [x] 지원 내용 요약 (benefit)
  - [x] 대상 조건 (eligibility)
  - [x] 연락처 (contact) - 전화 링크
  - [x] "자세히 보기" 버튼 (url 외부 링크)

### 페이지 조립

- [x] `app/(tabs)/signals/page.tsx` - Server Component에서 signals.json + policies.json import
- [x] `components/signals/signals-page.tsx` - "use client", 필터 상태 관리
- [x] 필터링 동작 확인: 카테고리 필터 + 지역 필터 + 등급 필터 적용

### 지역 지도 연동 (지역별 현황 통합)

- [x] `components/signals/region-map/region-map.tsx` - react-simple-maps 기반 지도
- [x] `components/signals/region-map/region-tooltip.tsx` - 호버 시 지역 정보 툴팁
- [x] 지역별 위기 등급 색상 표시 (danger/warning/caution/safe)
- [x] 지도 클릭 -> 필터 연동 (선택 지역 신호 필터링)
- [x] 지도-필터바 양방향 동기화

---

## Phase 5: 뉴스 분석 탭 UI + AI 분석 기능

### 검색/필터

- [x] `components/news/news-filter-bar.tsx` (검색 + 카테고리 통합)
  - [x] 키워드 검색 Input + 검색 아이콘
  - [x] 카테고리 필터 (Select: 전체 카테고리)

### 뉴스 목록

- [x] `components/news/news-list.tsx` (목록 래퍼)
  - [x] 빈 상태 처리 (검색 결과 없음)
- [x] `components/news/news-card.tsx`
  - [x] 상단: 카테고리 배지 (색상 구분) + 발행일
  - [x] 중앙: 기사 제목 + 요약 (2줄 제한)
  - [x] 하단: 키워드 태그 목록 (Badge)

### AI 분석 제어 패널

- [x] `lib/types.ts`에 분석 관련 타입 추가
  - [x] AnalysisPeriodPreset 타입
  - [x] AnalysisStep 인터페이스 (id, label, status, category)
  - [x] AnalysisProgress 인터페이스 (steps, currentStep, processedCount, totalCount, percent, elapsedSeconds, estimatedRemainingSeconds)
  - [x] AnalysisResult 인터페이스 (overallScore, severity, signalCount, elapsedSeconds)
  - [x] AnalysisState 타입 (idle/running/completed/error)
- [x] `lib/constants.ts`에 분석 상수 추가
  - [x] ANALYSIS_PERIOD_PRESETS 배열 (1w/1m/3m/6m/1y/all/custom)
  - [x] ANALYSIS_STEPS 배열 (7단계 정의)
- [x] `components/news/analysis-control-panel.tsx` (분석 제어 패널)
  - [x] 기간 프리셋 토글 그룹 (1주/1개월/3개월/6개월/1년/전체/직접설정)
  - [x] 직접 설정 시 시작일/종료일 DateInput 노출
  - [x] 카테고리 다중 선택 토글 (전체 + 5대 카테고리)
  - [x] 분석 대상 건수 실시간 표시
  - [x] 예상 소요 시간 표시
  - [x] "AI 분석 시작하기" CTA 버튼 (primary)
  - [x] 기간/카테고리 변경 시 하단 뉴스 목록 필터 연동

### AI 분석 진행 모달

- [x] `components/news/analysis-progress-modal.tsx`
  - [x] 진행 중 상태 UI
    - [x] 전체 진행률 Progress 바 (%)
    - [x] 처리 건수 / 전체 건수 텍스트
    - [x] 7단계 체크리스트 (완료/진행중/대기 아이콘 구분)
    - [x] 현재 처리 작업 설명 텍스트
    - [x] 경과 시간 / 예상 잔여 시간 표시
    - [x] 분석 취소 버튼
  - [x] 완료 상태 UI
    - [x] 종합 리스크 점수 + 등급 표시
    - [x] 감지된 위기 신호 수
    - [x] 소요 시간 표시
    - [x] "대시보드 보기" / "결과 확인" 버튼

### 뉴스 상세 모달

- [x] `lib/types.ts`에 `NewsArticleAnalysis` 인터페이스 추가
- [x] `NewsArticle`에 선택 필드 추가 (content?, source?, region?, analysis?)
- [x] `data/mock/news.json`에 content, source, region, analysis 데이터 추가 (10건)
- [x] `components/news/news-detail-modal.tsx` 생성
  - [x] 헤더: 카테고리 배지 + 섹션 + 출처 + 제목 + 날짜 + 지역
  - [x] 기사 원문 내용 (content ?? summary 폴백)
  - [x] 키워드 태그 목록
  - [x] AI 분석 결과 섹션 (위험도 바, 감지 신호, 핵심 요인, 연관 분야, 영향 지역, AI 요약)
  - [x] 분석 미실행 시 안내 문구 표시
- [x] `news-card.tsx`에 onClick 핸들러 + cursor-pointer 추가
- [x] `news-list.tsx`에 onArticleClick prop 전달
- [x] `news-page.tsx`에 selectedArticle/detailOpen 상태 + 모달 연동

### 분석 결과 캐러셀

- [x] `components/ui/carousel.tsx` 생성 (embla-carousel-react 기반)
- [x] `components/news/analyzed-news-slide.tsx` 생성
  - [x] 좌측 등급 색상 보더 (border-l-4)
  - [x] 카테고리 배지 + 출처 + 위험도 점수/등급
  - [x] 기사 제목 + AI 요약 (각 2줄 클램프)
  - [x] 감지 신호 + 키워드 태그
  - [x] 클릭 시 상세 모달 열기
- [x] `components/news/analyzed-news-carousel.tsx` 생성
  - [x] embla-carousel autoplay 플러그인 (5초 간격, 무한 루프)
  - [x] 마우스 호버 시 일시정지, 떠나면 재생
  - [x] 반응형: 모바일 1장 / sm 2장
  - [x] 좌우 네비게이션 화살표
  - [x] 섹션 헤더: "AI 분석 결과" + 건수 배지
- [x] `news-page.tsx`에 캐러셀 배치 (분석 제어 패널 아래, 필터 바 위)
  - [x] 분석 완료 뉴스 필터링 (analysis 있는 것만) + 위험도 내림차순 정렬

### 대시보드 향후 전망 제거

- [x] `ai-briefing-panel.tsx`에서 ForecastSection 렌더링 블록 제거
- [x] `forecast-panel.tsx` 파일 삭제

### 페이지 통합

- [x] `app/(tabs)/news/page.tsx` - Server Component에서 news.json import
- [x] `components/news/news-page.tsx` - "use client", 검색/필터 상태 관리
- [x] `components/news/news-page.tsx` - 분석 제어 패널 + 진행 모달 통합
  - [x] 분석 상태 관리 (idle/running/completed/error)
  - [x] Mock 분석 시뮬레이션 (단계별 진행 타이머)
  - [x] 기간/카테고리 선택 시 뉴스 목록 자동 필터링 연동
- [x] 검색 동작 확인: 제목 + 요약 + 키워드 배열에서 부분 일치 필터
- [x] 카테고리 필터 동작 확인

---

## Phase 6: 지역별 현황 (위기 신호 탭에 통합 완료)

> 지역별 현황 기능은 위기 신호 탭의 지역 지도로 통합되었습니다.
> 상세 구현은 Phase 4의 "지역 지도 연동" 섹션을 참조하세요.

- [x] 지역별 위기 현황 지도 (react-simple-maps)
- [x] 17개 시도 지역 표시
- [x] 지역별 위기 등급 색상 코딩
- [x] 지역 클릭 -> 신호 목록 필터링 연동
- [x] 호버 툴팁 (지역명, 점수, 신호 수)

---

## Phase 7: 차별화 기능 UI

### 7-A. AI 대화형 질의 ("이르미에게 물어보기")

- [x] `components/chat/chat-fab.tsx` - 플로팅 버튼 (우하단)
- [x] `components/chat/chat-panel.tsx` - 채팅 패널 메인
  - [x] isOpen 상태 관리
  - [x] messages 상태 관리
  - [x] isLoading 상태 관리
- [x] `components/chat/chat-header.tsx`
  - [x] "이르미에게 물어보기" 제목 + AI 아이콘
  - [x] 닫기 버튼
- [x] `components/chat/chat-messages.tsx` - 메시지 목록 영역
- [x] `components/chat/chat-message.tsx`
  - [x] user 메시지: 우측 정렬, primary 배경
  - [x] assistant 메시지: 좌측 정렬, secondary 배경
  - [x] 관련 신호 링크 표시
- [x] `components/chat/suggested-questions.tsx`
  - [x] 추천 질문 4개 버튼
  - [x] 초기 상태에서만 표시
- [x] `components/chat/chat-input.tsx`
  - [x] 텍스트 입력 + 전송 버튼
- [x] Mock 동작 구현
  - [x] chat-examples.json에서 키워드 매칭 로직
  - [x] 매칭 실패 시 기본 응답
  - [x] 타이핑 효과로 응답 출력
- [x] `app/(tabs)/layout.tsx`에 ChatFab 통합

### 7-B. 위기 연쇄 반응 맵 (차별화 #3)

- [x] `components/dashboard/crisis-chain-map.tsx` - 맵 메인 컨테이너
- [x] `components/dashboard/chain-map-header.tsx`
  - [x] "위기 전파 구조" 제목
  - [x] 활성 체인 수 표시
- [x] `components/dashboard/chain-visualization.tsx` - SVG 시각화
  - [x] 5개 카테고리 노드 (정오각형 배치)
  - [x] 노드별: 이름 + 점수 + 등급 색상
  - [x] 활성 노드: 펄스 애니메이션 (`scale`)
  - [x] 엣지 (노드 간 화살표) - strength별 선 굵기 차등
  - [x] 활성 체인 엣지: 흐르는 애니메이션 (`stroke-dashoffset`)
  - [x] 비활성 엣지: 회색 반투명
  - [x] 체인 선택 버튼 (활성 체인 강조)
- [x] `components/dashboard/chain-legend.tsx`
  - [x] 선 굵기 범례 (strong/moderate/weak)
  - [x] 색상 범례 (등급별)
- [x] `components/dashboard/chain-detail.tsx`
  - [x] 선택된 연쇄 경로 하이라이트
  - [x] 경로 설명 텍스트
  - [x] "물가(72) -> 자영업(81) -> 고용(58) -> 금융(63)" 형태
- [x] 인터랙션 구현
  - [x] 체인 버튼 클릭: 해당 연쇄 경로 하이라이트
  - [x] 활성 체인 자동 하이라이트 (페이지 로드 시)
- [x] dashboard-page.tsx에 crisis-chain-map 통합
- [x] globals.css에 chain-pulse, chain-flow 애니메이션 추가

### 7-C. 위기 연쇄 + 신호 통합 패널 (레이아웃 개선)

- [x] `components/dashboard/unified-crisis-panel.tsx` - 통합 메인 컨테이너
  - [x] 연쇄 맵 + 신호 사이드바 그리드 레이아웃 (md:grid-cols-5)
  - [x] 노드 클릭 시 해당 카테고리 신호 필터링
  - [x] 체인 선택 시 경로 내 모든 카테고리 신호 필터링
  - [x] 기본 선택: 최고 위험 카테고리 (자영업 81점)
- [x] `components/dashboard/signal-mini-card.tsx` - 컴팩트 신호 카드
- [x] `components/dashboard/signal-sidebar.tsx` - 필터링된 신호 목록
  - [x] 선택 컨텍스트에 따른 헤더 텍스트 변경
  - [x] 신호 통계 배지 (긴급/주의/관찰)
  - [x] "전체 신호 보기" 링크
- [x] `chain-visualization.tsx` 수정
  - [x] 노드 클릭 핸들러 추가 (onNodeSelect)
  - [x] 선택된 노드 시각적 표시 (점선 링 애니메이션)
- [x] `dashboard-page.tsx` 레이아웃 개선
  - [x] RecentSignals + CrisisChainMap -> UnifiedCrisisPanel 통합
  - [x] 빈 공간 제거, 전체 너비 활용

---

## Phase 8: API Route 스캐폴딩

### API Route 생성

- [x] `app/api/dashboard/route.ts` - GET, period 파라미터 지원
- [x] `app/api/signals/route.ts` - GET, category/region/severity 파라미터 지원
- [x] `app/api/signals/[id]/route.ts` - GET, 개별 신호 상세
- [x] `app/api/news/route.ts` - GET, keyword/category 파라미터 지원
- [x] `app/api/policies/route.ts` - GET, category/region/signalId 파라미터 지원
- [x] `app/api/analyze/route.ts` - POST, 뉴스 분석 (Mock 반환)
- [x] `app/api/chat/route.ts` - POST, AI 채팅 응답 (Mock 반환)
- [x] `app/api/briefing/route.ts` - GET, period 파라미터 지원 (Mock 반환)

### 공통

- [x] 응답 형식 통일: `{ success: boolean, data: T, error?: string }`
- [x] 쿼리 파라미터 기반 필터링 로직
- [x] `lib/api/response.ts` - API 응답 헬퍼 (successResponse, errorResponse)

### 유틸리티

- [x] `lib/api/mock-data.ts` - Mock JSON 데이터 로더 (필터링 포함)
- [x] `lib/api/anthropic.ts` - Claude API 클라이언트 (Mock 응답, 해커톤 당일 실제 API 교체)

### UI 전환

- [x] 각 페이지의 import 방식을 fetch 호출로 전환 가능 확인
  - [x] Dashboard: `/api/dashboard` 호출로 전환 가능 (dashboard + briefing + crisis-chain 통합)
  - [x] Signals: `/api/signals` 호출로 전환 가능 (region scores는 클라이언트에서 계산)
  - [x] News: `/api/news` 호출로 전환 가능
  - [x] Chat: `/api/chat` 이미 mock API 사용 중

---

## Phase 9: [해커톤 당일] 실데이터 교체 + Claude API 연결

> **핵심 파이프라인**: 원본 뉴스 JSON -> [호출1: 분류] -> [호출2: 분석] -> [호출3: 브리핑] -> 7개 JSON 생성
> `policies.json`, `chat-examples.json`은 뉴스 파생이 아닌 별도 유지 데이터

### [1] 환경 설정 (5분)

- [ ] `.env.local`에 `ANTHROPIC_API_KEY` 설정
- [ ] `NEWS_DATA_PATH`를 실제 JSON 경로로 변경

### [2] 데이터 교체 + 구조 확인 (10분)

- [ ] 제공된 뉴스 JSON 파일을 `data/` 디렉토리에 배치
- [ ] 원본 기사 JSON 구조 확인 (필드명, 인코딩)
- [ ] 필요 시 `RawNewsArticle` 타입 정의 (`lib/types.ts`)
- [ ] `news-data.ts` 로더의 파일 경로 전환

### [3] Claude API 연결 - 뉴스 -> JSON 생성 파이프라인 (40분)

#### 3-A. 파이프라인 기반 구조 구축

- [ ] `lib/api/anthropic.ts`에 Claude API 클라이언트 활성화 (주석 해제)
- [ ] `runAnalysisPipeline()` 오케스트레이션 함수 구현 (호출 1->2->3 순차 실행)
- [ ] API 호출 실패 시 기존 Mock JSON 반환하는 fallback 구조

#### 3-B. 호출 1: 뉴스 전처리 -> `news.json` 생성

- [ ] `classifyArticles()` 함수 구현
- [ ] 프롬프트 6-1 적용 (카테고리 분류 + 요약 + 키워드 추출 + 지역 식별)
- [ ] 원본 기사 -> `NewsArticle[]` 변환 검증
- [ ] 기사 수가 많을 경우 배치 분할 처리

#### 3-C. 호출 2: 종합 분석 -> 4개 JSON 동시 생성

- [ ] `analyzeRisks()` 함수 구현
- [ ] 프롬프트 6-2 + 6-3 통합 적용 (전체 맥락을 한번에 분석하여 일관성 확보)
- [ ] 생성 JSON 검증:
  - [ ] `signals.json` - 위기 신호 목록 (`Signal[]`)
  - [ ] `dashboard.json` - 종합 점수 + 카테고리별 점수/추세 (`DashboardData`)
  - [ ] `regions.json` - 17개 시도별 점수/이슈 (`RegionsData`)
  - [ ] `crisis-chain.json` - 카테고리 간 연쇄 관계 (`CrisisChainData`)
- [ ] `scoreHistory` 처리: 기존 목업 히스토리 유지 + `overallScore`만 마지막 항목으로 교체

#### 3-D. 호출 3: 브리핑 생성 -> `briefing.json`

- [ ] `generateFullBriefing()` 함수 구현 (호출 2 결과 기반)
- [ ] 프롬프트 6-4 + 6-6 통합 적용 (브리핑 + 향후 전망)
- [ ] `BriefingData` 타입 준수 검증 (`forecast` 필드 포함)

### [4] 페이지 데이터 소스 전환 + 스트리밍 (15분)

- [ ] 각 페이지의 mock import -> 파이프라인 결과 데이터로 교체
  - [ ] `app/(tabs)/page.tsx` - dashboard + briefing + crisisChain
  - [ ] `app/(tabs)/signals/page.tsx` - signals + regions
  - [ ] `app/(tabs)/news/page.tsx` - news
- [ ] `/api/briefing`에서 스트리밍 응답 -> 타이핑 효과 연결

### [5] AI 대화형 질의 연결 (20분)

- [ ] `/api/chat`에서 Mock 매칭 -> Claude API 호출 전환
- [ ] 대화 프롬프트 적용 (섹션 6-5)
- [ ] 현재 위기 데이터(호출 2 결과)를 system prompt 컨텍스트로 주입

### [6] 정책 데이터 보완 (10분)

- [ ] `policies.json` 정책 데이터 최신화 (URL, 연락처 확인)
- [ ] `relatedSignals` 매핑을 실제 생성된 신호 ID로 업데이트

### [7] 통합 테스트 및 발표 준비

- [ ] 파이프라인 전체 실행 -> 7개 JSON 정상 생성 확인
- [ ] 대시보드 -> 위기 신호 (지역 지도 포함) -> 뉴스 분석 전체 흐름 확인
- [ ] AI 브리핑 + 전망 정상 생성 확인
- [ ] AI 대화에 다양한 질문으로 테스트
- [ ] 위기 연쇄 반응 맵 애니메이션 확인
- [ ] 대응 가이드 + 정책 연결 정상 표시 확인
- [ ] 지역 지도 17개 시도 색상 정상 표시 확인
- [ ] 리포트 다운로드 기능 정상 동작 확인
- [ ] 실제 데이터 기반 UI 미세 조정
- [ ] 발표 데모 시나리오 리허설

---

## 품질 체크 (전 Phase 공통)

### 코딩 규칙 준수

- [ ] 색상 하드코딩 없음 (globals.css 시맨틱 토큰만 사용)
- [ ] 이모티콘/이모지 사용 없음 (HugeIcons만 사용)
- [ ] 환경변수 코드 내 직접 기입 없음
- [ ] `tailwind.config` 파일 미생성 (Tailwind v4 PostCSS 방식)
- [ ] `components/ui/` 내 파일 직접 편집 최소화
- [ ] 클라이언트에서 API 키 직접 접근 없음

### 아키텍처 준수

- [ ] Server Component 우선 사용 (데이터 fetch)
- [ ] Client Component는 인터랙션 필요 시에만 `"use client"` 선언
- [ ] 모든 컴포넌트가 props로 데이터를 받는 구조 (데이터 소스 교체 용이)
- [ ] `cn()` 유틸리티로 클래스 조합
- [ ] 반응형 디자인 (mobile-first: sm/md/lg)
- [ ] 다크모드 지원 (`dark:` variant)

### 발표 안전장치

- [ ] 모든 차별화 기능이 Mock 모드에서 완전 동작
- [ ] API 연결 실패 시에도 Mock 데이터로 데모 가능
- [ ] 전망 패널: Mock 텍스트로도 시나리오 비교 정상 표시
- [ ] 대응 가이드: Mock 정책 데이터에 실제 기관명/연락처 포함
